
# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# run the postprocessing 
#../../build/s_variables_processes &> s_variables_processes.log
#../../build/s_mixing_state_processes &> s_mixing_state_processes.log


# create output folder for initial run
mkdir -p out_init
# start the initial run
../../build/partmc urban_plume_init.spec &> run_init.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log

# create output folder for restart run
mkdir -p out
# start the restart run
../../build/partmc urban_plume_restart.spec &> run_restart.log
--------------------------------------------------------------------------
ORTE has lost communication with a remote daemon.

  HNP daemon   : [[64537,0],0] on node keeling-c16
  Remote daemon: [[64537,0],2] on node keeling-e08

This is usually due to either a failure of the TCP network
connection to the node, or possibly an internal failure of
the daemon itself. We cannot recover from this failure, and
therefore will terminate the job.
--------------------------------------------------------------------------
